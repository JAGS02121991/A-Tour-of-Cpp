

int main() {
    int x = 10;
    int y = 20;

    x + y;
    +x;
    x - y;
    -x;
    x * y;
    x / y;
    x % y;


    x == y;
    x != y;
    x < y;
    x > y;
    x <= y;
    x >= y;
    x & y;
    x | y;
    x ^ y;
    ~x;
    x && y;
    x || y;
    !x;

    x += y; // y = x + y;
    ++x; // x = x + 1;
    --x;
    x*=y;
    x/=y;
    x%=y;

    double d = 2.2;
    int  i = 7;

    d = d + i;
    i = d * i; // truncating double d * i  to an int
}