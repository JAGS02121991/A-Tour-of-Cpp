#include <math.h>
#include <iostream>

using namespace std;


int main() {
    constexpr int CONST_VAL = 20;

    const double CONST_SQRT = sqrt(10);

    cout << "CONST VAL: " << CONST_VAL << "\n" << "CONST SQRT: " << CONST_SQRT << "\n";
}